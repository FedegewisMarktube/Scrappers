# 🕸️ Web Scrapers Collection

This repository contains **multiple web scrapers**, each organized in its own folder.  
Every scraper includes:

- The **source code**  
- The **scraped data** (CSV, JSON, XLSX, etc.)  
- (Optional) a local `README.md` with specific details

---

## 📂 Project Structure

```bash
.
├── README.md
├── scrapers/
│   ├── computrabajo_ba/
│   │   ├── src/               # Scraper code
│   │   ├── data/              # Scraped data
│   │   └── README.md          # (Optional) extra documentation
│   ├── example_scraper_2/
│   │   ├── src/
│   │   ├── data/
│   │   └── README.md
│   └── ...
└── common/
    ├── utils/                 # Shared helper functions (if needed)
    └── config/                # Common config files (if needed)
